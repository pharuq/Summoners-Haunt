<%= form_for(current_user.get_friendship_id(@user).first,
                                                    html: { method: :delete },
                                                    remote: true) do |f| %>
  <%= hidden_field_tag :to_user_id, @user.id %>
  <%= button_tag type: :submit, class: "change-submit", data: { confirm: "友達を解除しますか？" } do %>
    <%= image_tag "/static_images/frined_sub_icon.png", alt: "sub_friend", class: "center-block" %>
  <% end %>
<% end %>
