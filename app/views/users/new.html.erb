<% provide(:title, '新規登録') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">新規登録</h3>
      </div>
      <div class="panel-body">
        <%= form_for(@user) do |f| %>
          <%= render 'shared/error_messages', object: f.object %>

          <%= f.label :name, "名前" %>
          <%= f.text_field :name, class: 'form-control' %>

          <%= f.label :email, "メールアドレス" %>
          <%= f.email_field :email, class: 'form-control' %>

          <%= f.label :password, "パスワード" %>
          <%= f.password_field :password, class: 'form-control' %>

          <%= f.label :password_confirmation, "パスワード（再入力）" %>
          <%= f.password_field :password_confirmation, class: 'form-control' %>

          <%= f.label :role, "ロール" %>
          <%= f.text_field :role, class: 'form-control' %>

          <%= f.label :profile, "プロフィール" %>
          <%= f.text_area :profile, rows: TEXT_AREA_BIG_ROW, class: 'form-control' %>

          <%= f.submit "登録", class: "btn btn-primary submit" %>

          <%= f.label :picture, "プロフィール画像" %>
          <span class="picture">
            <%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png', class: 'input_picture' %>
          </span>

          <div id="crop_area_box">
            <img id="crop_image" src="#" class="img-responsive hidden"/>
          </div>

          <%= hidden_field :user, :image_w, id: 'image_w' %>
          <%= hidden_field :user, :image_h, id: 'image_h' %>
          <%= hidden_field :user, :image_x, id: 'image_x' %>
          <%= hidden_field :user, :image_y, id: 'image_y' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
