<%= form_for(current_user.friendships.find_by(to_user_id: @user.id),
                                                    html: { method: :delete },
                                                    remote: true) do |f| %>
  <%= f.submit "友達解除", class: "btn" %>
<% end %>
