<%= form_for(current_user.active_pending_friendships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :to_user_id, @user.id %></div>
  <%# f.submit "友達申請", class: "btn btn-primary" %>
  <%= button_tag type: :submit, class: "change-submit", data: { confirm: "友達申請しますか？" } do %>
    <%= image_tag "/frined_add_icon.png", alt: "add_friend", class: "center-block" %>
  <% end %>
<% end %>
